#!/usr/bin/python3
"""Contain a function"""


def is_same_class(obj, a_class):
    """A function that check if object is an instance"""
    if (type(obj) is  a_class):
        return True
    else:
        return False
